export default function DashboardCard({ title, count }) {
  return (
    <div>
      <div>
        <p>{title}</p>
        <p>{count}</p>
      </div>
      <div>
        <img src="" alt="" />
      </div>
    </div>
  );
}
